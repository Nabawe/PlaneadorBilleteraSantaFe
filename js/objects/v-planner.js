let e_v_planner = _Q.qID( "v-planner--form" );

// Failsafe para frenar el submit
    // Didn't seam to work
/* if ( e_v_planner.addEventListener ) {
    e_v_planner.addEventListener(
        "submit",
        function( evt ) {
            evt.preventDefault();
            window.history.back();
        },
        true
    );
} else {
    e_v_planner.attachEvent(
        'onsubmit',
        function( evt ) {
            evt.preventDefault();
            window.history.back();
        }
    );
}; */
